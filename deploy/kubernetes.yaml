apiVersion: apps/v1
kind: Deployment
metadata: {name: ai-data-governance-platform}
spec:
 replicas: 2
 selector: {matchLabels: {app: ai-data-governance-platform}}
 template:
  metadata: {labels: {app: ai-data-governance-platform}}
  spec:
   containers:
   - name: api
     image: ai-data-governance-platform:latest
     ports: [{containerPort: 8000}]
     readinessProbe: {httpGet: {path: /health/ready, port: 8000}}
     livenessProbe: {httpGet: {path: /health/live, port: 8000}}
